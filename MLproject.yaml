name: singleoutput

python_env: python_env.yaml

entry_points:
  main:
    command: "python hyperparameters_tuning.py"
    parameters:
      config_file:
        type: str
        description: "Path to the configuration file"
        default: '../../../configs/singleoutput.yaml'
